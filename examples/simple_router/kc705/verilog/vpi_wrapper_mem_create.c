/*
 * Generated by Bluespec Compiler, version 2014.07.A (build 34078, 2014-07-30)
 * 
 * On Wed May 31 23:49:44 CST 2017
 * 
 * To automatically register this VPI wrapper with a Verilog simulator use:
 *     #include "vpi_wrapper_mem_create.h"
 *     void (*vlog_startup_routines[])() = { mem_create_vpi_register, 0u };
 * 
 * For a Verilog simulator which requires a .tab file, use the following entry:
 * $imported_mem_create call=mem_create_calltf size=64 acc=rw:%TASK
 * 
 * For a Verilog simulator which requires a .sft file, use the following entry:
 * $imported_mem_create vpiSysFuncSized 64 unsigned
 */
#include <stdlib.h>
#include <vpi_user.h>
#include "bdpi.h"

/* the type of the wrapped function */
unsigned long long mem_create(unsigned int *, unsigned int *, unsigned int *);

/* VPI wrapper function */
PLI_INT32 mem_create_calltf(PLI_BYTE8 *user_data)
{
  vpiHandle hCall;
  unsigned int *arg_1;
  unsigned int *arg_2;
  unsigned int *arg_3;
  unsigned long long vpi_result;
  vpiHandle *handle_array;
  
  /* retrieve handle array */
  hCall = vpi_handle(vpiSysTfCall, 0);
  handle_array = vpi_get_userdata(hCall);
  if (handle_array == NULL)
  {
    vpiHandle hArgList;
    hArgList = vpi_iterate(vpiArgument, hCall);
    handle_array = malloc(sizeof(vpiHandle) * 4u);
    handle_array[0u] = hCall;
    handle_array[1u] = vpi_scan(hArgList);
    handle_array[2u] = vpi_scan(hArgList);
    handle_array[3u] = vpi_scan(hArgList);
    vpi_put_userdata(hCall, handle_array);
    vpi_free_object(hArgList);
  }
  
  /* create return value */
  make_vpi_result(handle_array[0u], &vpi_result, DIRECT);
  
  /* copy in argument values */
  get_vpi_arg(handle_array[1u], &arg_1, POLYMORPHIC);
  get_vpi_arg(handle_array[2u], &arg_2, POLYMORPHIC);
  get_vpi_arg(handle_array[3u], &arg_3, POLYMORPHIC);
  
  /* call the imported C function */
  vpi_result = mem_create(arg_1, arg_2, arg_3);
  
  /* copy out return value */
  put_vpi_result(handle_array[0u], &vpi_result, DIRECT);
  
  /* free argument storage */
  free_vpi_args();
  vpi_free_object(hCall);
  
  return 0;
}

/* sft: $imported_mem_create vpiSysFuncSized 64 unsigned */

/* tab: $imported_mem_create call=mem_create_calltf size=64 acc=rw:%TASK */

PLI_INT32 mem_create_sizetf(PLI_BYTE8 *user_data)
{
  return 64u;
}

/* VPI wrapper registration function */
void mem_create_vpi_register()
{
  s_vpi_systf_data tf_data;
  
  /* Fill in registration data */
  tf_data.type = vpiSysFunc;
  tf_data.sysfunctype = vpiSizedFunc;
  tf_data.tfname = "$imported_mem_create";
  tf_data.calltf = mem_create_calltf;
  tf_data.compiletf = 0u;
  tf_data.sizetf = mem_create_sizetf;
  tf_data.user_data = "$imported_mem_create";
  
  /* Register the function with VPI */
  vpi_register_systf(&tf_data);
}
